# Competitor feature matrix & adoption backlog

*Surveyed June 2026. Complements `01-research-and-architecture.md` (field survey);
this doc drives the post-v0.1 feature backlog: what the incumbents do best and
which of those features we adopt, in what order.*

## The field

| Product | Platform | Strengths to steal | Weaknesses we exploit |
|---|---|---|---|
| **Frigate 0.16** | Linux/Docker | Two-stage motion→AI (our model already), zone/mask editor, review items split into alerts vs detections, event clips, sub-stream "detect role", MQTT events, face/LPR (now free) | No native Windows/macOS; needs Docker + accelerator setup |
| **Blue Iris 5** | Windows only | 64-camera scale, alarm-server webhooks on events, ONVIF events/PTZ, audio recording, profiles/schedules, &Deadband-style alert tuning | Windows-only, dated UI, per-seat license |
| **UniFi Protect (G6 era)** | UniFi hardware only | Timeline scrubbing UX, smart detect (person/vehicle/pet), camera health/status surface, storage dashboard | Closed ecosystem — only UniFi cameras |
| **Scrypted** | Node, cross-platform | Plugin architecture, HomeKit/Alexa/Google bridging, fast WebRTC | AI depends on plugins/cloud; core is a bridge more than an NVR |
| **ZoneMinder / Shinobi / MotionEye** | Linux | Long-tail camera support, mature retention models | Aged stacks, weak/no native AI, heavy setup |

## Feature adoption backlog (ranked)

| # | Feature | Inspired by | Status |
|---|---|---|---|
| 1 | Close-to-tray desktop NVR semantics | Blue Iris service mode | ✅ shipped |
| 2 | Bundled ffmpeg/go2rtc/model in installer | Blue Iris all-in-one install | ✅ shipped |
| 3 | Event → recording playback at timestamp | UniFi timeline, Frigate review | ✅ shipped |
| 4 | Camera health (online/offline, last frame, fps) | UniFi status surface, BI watchdog | ✅ shipped |
| 5 | Per-camera detect config: labels, thresholds, ignore zones | Frigate zones/masks, BI per-camera profiles | ✅ shipped |
| 6 | Webhook on event (alarm-server style) | Blue Iris alarm server, Frigate MQTT | ✅ shipped |
| 7 | Audio in recordings (AAC) | Blue Iris | ✅ shipped |
| 8 | Storage dashboard (per-camera usage, est. days left) | UniFi | ✅ shipped |
| 9 | Sub-stream detect role (decode low-res, record 4K) | Frigate detect role, BI dual-streaming | ✅ shipped (validated on real Dahua 4K) |
| 10 | Timeline scrubber UI across segments + event markers | UniFi Protect | ✅ shipped |
| 11 | Review split: alerts (person/car in zone) vs detections | Frigate 0.14 | ✅ shipped |
| 12 | Event clips (pre/post-roll MP4 export) | Frigate clips, BI export | ✅ shipped |
| 13 | ONVIF discovery + PTZ | Blue Iris, Frigate 0.16 | ✅ shipped (validated on Amcrest IP2M-866EW) |
| 14 | Face recognition / LPR | Frigate 0.16 (free since 0.16) | ✅ both shipped (faces validated live; LPR char-accurate on stills, validated on live stream) |
| 15 | MQTT broker integration (Home Assistant) | Frigate | ✅ shipped (verified against local broker) |
| 16 | Auth + HTTPS for off-LAN exposure | all commercial | ✅ shipped & WAN-hardened (argon2id password hashing with transparent upgrade-on-login from legacy SHA-256; per-IP login brute-force throttle → 429 + Retry-After after 8 wrong tries in 5 min (loopback exempt; map swept + capped at 4096 IPs so address rotation can't exhaust memory); native HTTPS via rustls with one-flag `--tls-self-signed` (auto self-signed cert under `<data>/tls`, key `0600`/dir `0700` on Unix, reused across runs) or explicit `--tls-cert`/`--tls-key`; session cookie gains `Secure` over TLS; opt-in `--trusted-proxy` keys auth+throttle off the right-most `X-Forwarded-For` hop so a same-host reverse proxy's loopback connection can't inherit the local-access exemption (and a spoofed `XFF: 127.0.0.1` can't either). TLS pinned to the **ring** crypto provider already in-tree (ureq/rumqttc/rcgen) — no OpenSSL, no new aws-lc-sys/NASM build dep, 3-OS CI stays green. Live-validated over TLS: ring handshake, argon2 login, Secure cookie, wrong-password 401, plain HTTP to the TLS port refused; with `--trusted-proxy`: proxied client forced to auth, spoofed-loopback rejected, XFF-keyed lockout fires on the 9th attempt with Retry-After while a second IP stays unlocked) |
| 17 | Natural-language smart search (CLIP) | UniFi AI Key | ✅ shipped (validated ranking on live events) |
| 18 | PTZ autotracking | Frigate 0.13 | ✅ shipped (closed loop validated in daylight on the Amcrest: car detected below center → tilt-down burst → frame re-centered, 38% pixel shift; velocity floor 0.4 + offset-scaled burst added because the motor ignores small velocities over short bursts) |
| 19 | Audio event classification (YAMNet) | Frigate, UniFi AI | ✅ shipped (validated live: tone -> alarm event @0.92 with snapshot) |
| 20 | Enhanced Retention (re-encode aging footage) | UniFi Protect | ✅ shipped (validated: 16.4MB segment -> 2.5MB, playable) |
| 21 | One-click network camera scan (WS-Discovery) | Blue Iris "Find", Synology camera search | ✅ shipped (validated: found 7 LAN cameras incl. both real ones; probes every local interface to survive WSL/Hyper-V multi-homing) |
| 22 | Phone push notifications via ntfy (snapshot attached) | UniFi/Reolink push, Frigate notify add-ons | ✅ shipped (validated live: rule fired, ntfy.sh delivered title + message + snapshot.jpg) |
| 23 | Configurable storage location + free-space display | Blue Iris multi-drive clips, Synology volumes | ✅ shipped (validated: segments wrote + indexed to alt dir, recorders auto-restart on dir change, bad path rejected with 400, free space shown) |
| 24 | Alarm rule schedules (days + time window, overnight) | Blue Iris profiles/schedules | ✅ shipped (validated live: in-window rule fired 5x while night-window and wrong-day rules on the same events stayed silent; unit tests cover overnight + day filters) |
| 25 | Event-only recording retention (per camera) | Frigate retain modes | ✅ shipped (validated live: swept 235 eventless porch segments after 15-min grace, kept the segment with a nearby event) |
| 26 | PWA install (manifest + icons, add-to-home-screen) | UniFi/Reolink mobile apps | ✅ shipped (manifest served as application/manifest+json with 192/512 + maskable icons, apple-touch-icon, theme color) |
| 27 | Camera disconnect push alerts (offline / back online) | UniFi Protect, Reolink, Blue Iris watchdog | ✅ shipped (validated live: killed a camera's source → "Camera offline" ntfy push with the error, restored it → "back online" push; intentionally disabled cameras don't alert) |
| 68 | Native browser push notifications (no third-party push service) | UniFi Protect / Reolink / Ring mobile push, Frigate notify add-ons — but those route through a cloud relay; this is self-contained | ✅ shipped (full **Web Push** stack on `ring`, already in-tree for TLS — **zero new crates**, verified `Cargo.lock` unchanged: VAPID (RFC 8292) = a persistent P-256 keypair, each request carrying an ES256 JWT; payload encryption (RFC 8291 `aes128gcm`) = a per-message ephemeral P-256 ECDH against the subscription's `p256dh`, mixed with its `auth` secret via HKDF-SHA256, then AES-128-GCM in RFC 8188 framing. A polling worker (`push.rs`) watches the `notifications` table — the single sink every alert source (alarms, camera offline/online, anomaly, digest) already writes to — and fans each new row out to all subscribed browsers, auto-pruning any endpoint that returns 404/410. `GET /api/push/vapid` + `POST /api/push/{subscribe,unsubscribe,test}` (Viewer-reachable; per-browser opt-in from Settings); the service worker shows the notification + focuses the app on click. **Crypto correctness PROVEN: `derive_keys`+`build_body` reproduce the RFC 8291 Appendix A test vector byte-for-byte; the VAPID JWT signs+verifies — both unit-tested. Live: a real FCM endpoint returned 404, i.e. it accepted our VAPID auth + `aes128gcm` encoding (a bad JWT → 401, bad encoding → 400). The one piece needing a real device: the browser "Allow notifications" gesture + a live push token, which automation can't grant.**) |
| 61 | Cross-camera appearance search (Re-ID — "find this person/vehicle everywhere") | Verkada / Genetec / Avigilon "Appearance Search", UniFi Protect smart search | ✅ shipped (each object detection's **crop** (not just the full frame) is CLIP-embedded at detection time and stored alongside the existing text-search frame embedding — reusing the single loaded CLIP session, no new model dependency. `GET /api/events/{id}/similar` ranks every other event by cosine similarity of its crop embedding against the query event's, returning matches **across all cameras and time**, newest history first (retention-bounded, no recall cap). An Events-page "Similar" button opens a modal showing the query crop beside the ranked matches (similarity %, camera, time; click-through to the match). Degrades gracefully: `available:false` when an event has no crop embedding (not an object detection, or the CLIP models aren't installed). Live-validated end-to-end on two cameras: a query person ranked its 4 same-appearance matches across **both** cameras (0.91–0.97) above 3 distractors (0.11–0.22); `db::crop_embeddings`/`crop_embedding_for` unit-tested) |
| 60 | Activity heatmap (where objects concentrate over time) | Axis/Hikvision retail heatmaps, Avigilon, Verkada activity maps | ✅ shipped (`GET /api/analytics/heatmap?camera=&from=&to=&grid=` accumulates each object detection's ground-anchor (bottom-centre of the box) into a `grid`×`grid` density map over an optional time range — a foot-traffic heatmap of where objects actually stood; synthetic analytics-marker events (crossing/wrong_way/loiter/occupancy/gesture) and degenerate zero-area boxes (audio events) are excluded so it reflects real object presence. Read-side only — derived from the existing events, no extra pipeline cost (`db::heatmap`, unit-tested incl. the exclusions). A canvas overlay on the camera detail page renders it on the dimmed live frame with a blue→red colour ramp + a Today/24h/7d/30d/All range selector; live-validated end-to-end (DB→endpoint→canvas hotspots)) |
| 59 | People / vehicle counting (throughput in/out/net) | Axis/Hikvision/Bosch people counters, UniFi Protect counting, retail footfall | ✅ shipped (the tripwire crossings (#54) are aggregated into a true throughput roll-up at `GET /api/analytics/counts` — per tripwire, `a_to_b` = "in", `b_to_a` = "out", net = in − out — over an optional time range, derived from the `crossing`/`wrong_way` events (one physical pass = one event, so these are real counts, not cooldown-inflated detection tallies). A wrong-way pass still counts toward throughput. Surfaced as a "Throughput today" Overview card (in/out/net per line). Read-side only — no extra pipeline cost) |
| 58 | Live zone occupancy + capacity alarm (max-occupancy / crowd) | Axis/Bosch occupancy, UniFi Protect, retail/COVID capacity, fire-code crowd limits | ✅ shipped (the analytics tick now also reports, per polygon zone, the **live count** of confirmed tracks (#53) currently inside it (honouring the zone's label scope), published each frame to the status board and exposed at `GET /api/analytics/occupancy` + a "Live occupancy" Overview card. A per-zone `occupancy_max` limit arms an **edge-triggered** `occupancy` event the instant the live count first exceeds it (re-armed when it drops back — one alert per breach, not per frame), firing the existing alarm/webhook/MQTT machinery via the label match. Per-zone limit field in the ZoneEditor; the tracker turns on for a zone only when it carries a limit (opt-in, no cost on plain zones). Unit-tested count + edge-trigger + label-scope; **live-validated**: 4 tracks counted in a zone, capacity alarm fired exactly once) |
| 57 | Wrong-way / one-way enforcement | Bosch IVA, Genetec, Avigilon, Eagle Eye (exit-only doors, one-way corridors, wrong-way drivers) | ✅ shipped (a directional tripwire (#54) gains `alert_wrong_way`: a crossing in the *forbidden* direction fires a first-class `wrong_way` event instead of being silently suppressed, riding the existing alarm/webhook/MQTT machinery (an alarm rule with `label = "wrong_way"` fires). Pure trajectory logic on the tracker (#53); per-tripwire toggle in the ZoneEditor (disabled for both-direction lines). Unit-tested: the forbidden direction fires + is marked wrong-way, the allowed direction stays a normal crossing) |
| 56 | Speed estimation (calibrated; pairs with LPR for driveway/road speeding) | Frigate speed-threshold, Axis/Bosch/Genetec speed, traffic-cam speeding | ✅ shipped (a per-camera ground-plane calibration — mark 4 image corners of a known ground rectangle + its real width × length — solves a 3×3 image→ground homography (`tracker::Homography`, hand-rolled 8×8 Gaussian DLT, unit-tested incl. a perspective-trapezoid round-trip + degenerate rejection). `analytics::track_speed_kmh` warps a tracked object's recent trajectory to the ground plane and computes km/h over the last few seconds (curved-path-aware via summed segments, noise-capped). Speed rides onto `crossing`/`wrong_way` events (new `events.speed` column) + the webhook payload. Calibration UI draws an amber ground rectangle on the live frame with width/length-in-metres inputs. Pure math, fully headless-tested (a calibrated synthetic track at a known speed reads back ~18 km/h); the detection→crossing path needs a real moving object to exercise end-to-end) |
| 55 | Loitering / dwell-time alerts | Frigate loitering, Axis "Time in area", Bosch/Genetec idle-object | ✅ shipped (a `PolyZone.dwell_secs` threshold turns any polygon zone into a dwell zone: a tracked object whose ground-contact point stays inside for longer than the threshold fires a first-class `loiter` event once, with an occlusion grace window so a track briefly lost to occlusion doesn't reset its timer. Built on the new multi-object tracker (#53), reuses the existing point-in-polygon zone test, and rides the existing snapshot + webhook + MQTT + alarm machinery (an alarm rule with `label = "loiter"` fires). Per-zone dwell field in the ZoneEditor. 9 analytics unit tests cover the threshold/pass-through/grace/GC cases; logic is deterministic and fully headless-tested) |
| 54 | Line-crossing / virtual tripwires with direction + true in-out counting | Axis Object Analytics, Bosch IVA, Genetec/Avigilon, Synology, Meraki — the canonical commercial perimeter/counting primitive | ✅ shipped (a directed `Tripwire` (frame-fraction line segment + direction filter `both`/`a_to_b`/`b_to_a` + object-label filter); a confirmed track whose ground-contact anchor flips sides of the line — sign of the 2-D cross product — crosses it, classified by direction, firing a first-class `crossing` event with a new `direction` column. Built on the new tracker (#53) so one physical pass = one crossing (not one-per-frame). True throughput counts (tracker-deduped, unlike the cooldown-inflated detection tallies) via `GET /api/analytics/counts`, grouped by tripwire + direction. ZoneEditor draws tripwire lines + a per-line direction/labels list; an alarm rule with `label = "crossing"` fires on any crossing. Pure geometry — unit-tested (direction, one-way suppression, no-cross, label filter); config + counts API round-tripped live; tripwire-drawing validated in Chrome over a live frame) |
| 53 | Multi-object tracker (persistent track IDs + trajectories) — the analytics foundation | Frigate/Axis/Verkada/Bosch IVA/Genetec/Avigilon all build their analytics suite on a tracker | ✅ shipped (new pure-Rust `crates/tracker`: a SORT-lite multi-object tracker — constant-velocity box prediction → greedy IoU association in a ByteTrack-style two pass (confident detections first, then a low-confidence recovery pass that rescues partial occlusions but never spawns new tracks) → hit/miss hysteresis to confirm/retire, with bounded centroid trajectories. Coordinates are frame fractions so tracks share the zone/tripwire space; the bottom-center ground-contact anchor cuts perspective error. Wired per-camera into the detection pipeline, fed the full label/score/zone-filtered detection set, active only when a tripwire/dwell zone is configured. **This was Cammy's one foundational gap vs commercial analytics** — the detector was per-frame only (no object identity), so line-crossing/loitering/counting/occupancy/speed/heatmaps were impossible; this unlocks the whole class. 8 unit tests: ID stability through a crossing, occlusion survival + re-association, retirement after max-age, min-hits confirmation, label separation, IoU/anchor geometry. No FFI/GPU; fully headless-tested) |
| 52 | Security audit log (logins, password & token changes) | Blue Iris/UniFi sign-in logs — WAN-exposure accountability | ✅ shipped (a bounded `audit_log` table records security events — `login_success`/`login_failed` (with client IP, resolved the same way as the throttle so it's correct behind a trusted proxy), `password_set`/`password_cleared`, `token_created`/`token_revoked` — and `GET /api/audit` lists them newest-first. **No secrets are logged** (only the action + token *name*, never a password or raw token). The table self-trims to the most recent 2000 rows so a flood of (already-throttled) failed logins can't grow it unbounded. Settings gains a "Recent security activity" card. **The audit log is session-only** — `token_forbidden` blocks a Bearer API token from reading it (review-driven), so a leaked token can't reconnoiter login IPs or which other tokens exist. `db::add_audit` is best-effort (a logging failure never blocks the audited action). **Live-validated:** set-password, a wrong then correct login, and a token create all appeared with the right action/IP/detail. Focused review: 0 defects above the (applied) session-only hardening) |
| 51 | Stranger / unfamiliar-face detection (alert on unknown people) | UniFi Protect "unfamiliar face", Nest "unfamiliar" — the marquee smart-NVR feature | ✅ shipped (a person whose face is detected but matches **no enrolled identity** is tagged as a stranger — event `face` = the reserved `UNKNOWN_FACE` sentinel — and a new Alarm Manager condition `face_unknown` fires a webhook/ntfy/MQTT action on it. In `pipeline::run_faces`, an unmatched confident face marks the containing person box only if it isn't already a recognized identity (a real name always wins) **and only when ≥1 identity is enrolled** — with none, *everyone* is "unknown", which is noise, not a stranger (review-driven guard against a first-run flood). The enroll/rename endpoints reject the reserved sentinel name. `AlarmRule.face_unknown` (migration + matches() check, reusing the face param); Alarms page gains an "unknown face (stranger)" condition (mutually exclusive with face-name match, with an enroll-first hint); Events page renders the sentinel as "🚶 stranger". **Live-validated end-to-end on a real USB webcam: an un-enrolled face → `person` event `face="?"` (84%) → the `face_unknown` webhook fired** (`label=person face='?'`). Also validated on real IP hardware (Dahua IPC-HDW4231EM-AS + Amcrest IP2M-866EW over ONVIF→RTSP, GPU/DirectML detection). Adversarial 2-lens review (correctness + false-positive) drove the zero-enrolled guard, the reserved-name rejection, and the face_like/face_unknown exclusivity) |
| 50 | Event CSV export | Blue Iris/UniFi event-log export — record-keeping, insurance, sharing | ✅ shipped (`GET /api/events/export.csv` downloads matching events as RFC 4180 CSV — same filters as the events list (camera/label/gesture/zone/time-range/flagged), up to a generous cap, with a `Content-Disposition` attachment. Columns: id, local time, camera, label, score, face, plate, gesture, zone, flagged, note, caption, transcript. The renderer **guards against spreadsheet formula injection** (a field starting with `= + - @` is prefixed with `'`) since transcripts/captions/notes are partly attacker-influenced; pure `events_to_csv`/`csv_field` are unit-tested (RFC 4180 quoting + the formula guard). Events page gains a "⬇ Export CSV" link that carries the active filters. **Live-validated:** correct `text/csv` + attachment headers, header row + event rows, and the `flagged=true` filter narrows the export) |
| 49 | Prometheus metrics endpoint (`/api/metrics`) | Frigate exposes Prometheus metrics; UniFi/Blue Iris don't — observability for self-hosters | ✅ shipped (`GET /api/metrics` returns Prometheus 0.0.4 text exposition: `zoomy_build_info`, `zoomy_cameras`/`_online`, `zoomy_events`, `zoomy_disk_free_bytes`, and per-camera gauges `zoomy_camera_online`/`_recording`/`_storage_bytes`/`_segments`/`_inference_ms`/`_last_frame_age_seconds` (labelled by camera). Data comes from the segment index + status board + event count — no new dependency, hand-rendered. The exposition builder (`render_metrics`) is pure + unit-tested incl. label escaping. Gated by the same auth as the rest of `/api`, so a scraper authenticates with an **API token** (#48) via `Authorization: Bearer` (not blocked by the token-escalation gate) or runs on the loopback box. **Live-validated:** valid exposition with the right `text/plain; version=0.0.4` content-type and per-camera series; remote scrape without auth → 401. A focused review found 0 defects above a pre-existing low) |
| 48 | API access tokens (bearer auth for headless callers) | Frigate/Scrypted API access, Home Assistant integrations — automation enablement | ✅ shipped (bearer tokens let scripts/integrations call the JSON API from another host without the session cookie. `POST /api/tokens {name}` mints a `zoomy_<64-hex>` (256-bit) token, returns it **once**, and stores only its SHA-256 hash; `GET /api/tokens` lists metadata (name/created/last-used, never the secret); `DELETE /api/tokens/{id}` revokes. `auth::middleware` accepts `Authorization: Bearer <token>` (scheme matched case-insensitively per RFC 7235) after the existing session/loopback checks, stamping last-used at most once/min. **Hardening from the security review: a Bearer token is denied token-management + password-change** (`/api/tokens` POST·DELETE and `/api/auth/password` 403 for token callers — only an interactive session/loopback can), so a leaked token can't mint siblings that survive revocation or lock the owner out. Settings "API tokens" card (create/one-time-reveal/list/revoke). **Live-validated via `--trusted-proxy`+XFF:** remote 401 without auth, 200 with token, 401 bad/revoked, 403 on the escalation endpoints, loopback unaffected. Token-hash + escalation-gate + CRUD unit-tested) |
| 47 | Event bookmarks (flag + note, protected from retention) | UniFi Protect bookmarks, Blue Iris "protect" flag — the event-review workflow | ✅ shipped (a per-event `flagged` + free-text `note`; `POST /api/events/{id}/bookmark` and a server-side `flagged` list filter. **A bookmarked event is exempt from the event-retention prune** — `prune_events_before` keeps flagged rows *and* their snapshots (the snapshot-delete query skips files still referenced by any flagged event), so a saved clip survives past the retention window. Events page: a ★/☆ save toggle + 📝 note per card (note display + a "⭐ Saved" filter); un-saving a noted event confirms then drops the note so a note is never orphaned. The bookmark endpoint distinguishes absent (preserve note) / null·"" (clear) / string (set, ≤500 chars) via a custom serde deserializer. **Live-validated via the API:** flag+note persists, the `flagged=true` filter returns it, 404 on a missing id, unflag clears, and note preserve/clear/cap all behave; retention protection + the shared-snapshot case are unit-tested. Adversarial 2-lens review (correctness + UX) caught the note-orphan-on-unflag bug and the absent-note-wipe footgun, both fixed) |
| 46 | Spoken-keyword alarm (a heard "safe word" triggers an action) | Differentiator — pairs #44 transcription with Alarm Manager; the audio sibling of the #35 duress gesture. No NVR fires on *spoken* words | ✅ shipped (a new Alarm Manager condition `transcript_like` — case-insensitive substring on an event's speech-to-text transcript — fires a webhook/ntfy/MQTT action when a phrase is *said* near a camera, e.g. a spoken "help"/"fire". Evaluated **only** in the transcribe worker after whisper writes the transcript (`fire_transcript_alarms`), guarded so it never double-fires against the audio-event dispatch; rules without a phrase are untouched, and an empty phrase can't become an always-firing rule. The matched transcript rides into the webhook JSON / ntfy push text / template `{{transcript}}` placeholder so the receiver sees what was said. Alarms page gains a "spoken phrase" field. **Live-validated end-to-end:** a spoken "americans" fired the rule exactly once and delivered the webhook carrying `transcript="And so my fellow Americans ask not what you are"`. Adversarial 2-lens review (correctness/double-fire + security) → removed a redundant clippy attribute, hardened empty-phrase handling, and `\u`-escaped C0 control chars in webhook templates; 0 real defects above that) |
| 45 | Transcript-aware smart search (search what was said, not just seen) | Extends #17 CLIP search with #44 transcripts — a combination no NVR offers | ✅ shipped (the ✨ Events search is now **hybrid**: CLIP visual similarity on the snapshot **plus** a whole-word text match on each event's transcript + caption, so you can search spoken words / descriptions — "someone yelling help" — and a speech/caption hit always outranks a pure-visual one. Now also **works without the CLIP models** (degrades to a transcript/caption text search instead of erroring). New `smart::text_match_score` (lowercased, whole-word tokenized so "car" can't match "scared", with a multi-word phrase shortcut; unit-tested); `db::search_corpus` joins event text + the optional embedding in one query over the full (retention-bounded) history — no recall cap, and the embedding column is skipped entirely in text-only mode. Results carry a `match: speech|visual` tag and a `mode: hybrid|text`. **Live-validated:** searching "americans" surfaced the jfk transcript event at the top (`match=speech`); a word absent from the stored clip correctly didn't match. Adversarial 2-lens review (correctness/perf) drove uncapping recall, the text-only embedding skip, signal-only result filtering, and whole-word matching; 0 high findings) |
| 44 | Bundled audio transcription (speech-to-text on audio events) | Differentiator — no mainstream NVR transcribes; UniFi/Frigate stop at sound class | ✅ shipped (opt-in, **off by default**, fully **local** — whisper.cpp is **compiled into the binary** via whisper-rs, no separate server/process. When enabled, a YAMNet audio event triggers a worker that captures a short clip from the camera's loopback restream and writes a speech-to-text transcript onto the event — shown on Events cards (🎙️) and searchable. New `crates/core/src/transcribe.rs` (worker loads the model once; capture is watchdog-killed so a stalled stream can't hang shutdown; transcript body logged at debug, only metadata at info — no spoken words in logs); per-camera gating via the existing `audio_detect`; `transcript` event column; Settings card + model path (`ggml-tiny.en.bin`, downloaded not committed). **Live-validated end-to-end with the bundled model: a jfk.wav "intercom" camera → YAMNet "Speech" event → transcript "And so my fellow Americans ask not what you are" on the event card** (+ a unit test transcribing the sample). Builds on all 3 OSes — CI sets per-OS whisper deps (Linux uses the crate's shipped bindings so no libclang; Windows points bindgen at the runner's LLVM with a choco fallback; macOS uses Xcode's libclang); cmake is preinstalled on runners. README documents the cmake/LLVM build prereqs. Adversarial 3-lens review (build-CI/correctness/privacy) drove a capture timeout + Windows-CI robustness fix; 0 high findings) |
| 43 | Two-way audio / push-to-talk | UniFi/Reolink/Frigate two-way talk; doorbell intercom | ✅ shipped (a per-camera **opt-in** `two_way_audio` (DetectConfig flag + tuning-page toggle) adds a **hold-to-talk** button to the camera detail view. Holding it streams the browser mic to the camera over WebRTC: the go2rtc `<video-rtc>` player adds a **send-only audio track** (`getUserMedia` → `addTransceiver(sendonly)`) that rides the #42 `/api/ws` reverse-proxy to go2rtc, which routes it to the camera's backchannel. Forces WebRTC while talking (MSE/MJPEG can't carry an upstream track). **Live-validated in Chrome against a real USB-webcam mic:** pressing talk negotiated a `sendonly` audio transceiver with the live mic track; the red "🎙️ Talking…" state showed over live video; **releasing stops the mic in ~100 ms** — the adversarial review caught that go2rtc's player defers its own track-stop behind a 5 s timer (mic would stay hot after release), so cleanup now calls `sender.track.stop()` immediately, plus a window-level pointer-up/blur safety net guarantees release. Opt-in because it needs a camera with a speaker / ONVIF backchannel; the speaker-playout end needs such a camera to validate (the webcam has none). Brief video re-negotiation on press/release is inherent to the player's media-at-offer design (hold ~1 s before speaking)) |
| 42 | Remote live-view via authenticated WebSocket reverse-proxy | UniFi/Frigate remote streaming; closes the #41 local-only gap | ✅ shipped (the live player's WebSocket now connects to zoomy's **own origin** `/api/ws?src=NAME`, which reverse-proxies to the loopback-only go2rtc — browser ⇄ zoomy ⇄ go2rtc. This (a) makes **remote/LAN live-view work** — MSE/MJPEG media flows over the proxied socket so any viewer, not just one on the server box, gets video; (b) lets us **drop `origin: "*"`** so go2rtc keeps its default same-origin protection (closes the localhost-origin CSRF the #41 review flagged); (c) routes live streams through zoomy's **auth middleware** like every other `/api/*` route (a password now also gates live view; loopback stays exempt). `stream_ws`/`proxy_ws` build the upstream URL from the fixed loopback base + only the urlencoded `src` (no SSRF), pump both directions with `tokio::select!`, bound the upstream connect with an 8s timeout, reject an empty `src`, and send a clean Close on upstream failure. Pure-Rust deps (tokio-tungstenite 0.29 — deduped to match axum's — + futures-util; no TLS, no C). **Live-validated in Chrome against a real USB webcam: WebRTC and MSE both play 640×480 over `ws://localhost:8080/api/ws` (same origin, not go2rtc:1984), go2rtc.yaml has no `origin`, empty-src → 400.** Adversarial 2-lens review (security+async): SSRF "correctly closed", resource cleanup "sound — no leak", 0 real defects above nit; the connect-timeout / empty-src / clean-close robustness nits were folded in) |
| 41 | Native live-view player (drop the per-camera iframe) | UniFi/Frigate embedded players; CLAUDE next-step #2 | ✅ shipped (the Live grid + camera detail now embed go2rtc's `<video-stream>` web component — a real `<video>` element with WebRTC and automatic MSE/MJPEG fallback — instead of an `<iframe>` onto go2rtc's stream.html. Lower overhead, proper sizing, no nested-page chrome; the existing per-viewer transport selector maps to the component's mode priority. go2rtc serves the player JS without CORS, so a thin same-origin `GET /api/player/{file}` proxy (allowlisted to video-stream.js/video-rtc.js) serves it from our origin. go2rtc's API is now bound to **127.0.0.1** (was `0.0.0.0` — a net reduction in exposure, off the LAN) with `origin: "*"` so the same-machine UI can open the player WebSocket cross-origin. **Live-validated in Chrome against a real USB webcam: WebRTC and MSE both play live 640×480 video, "RTC"/"MSE" mode badges, clean console** (MJPEG is declined by go2rtc for an H.264-only source — `codecs not matched` — same as under the old iframe). The module-load promise caches only on success so a go2rtc restart can't permanently black out tiles. **Known limits (pre-existing, documented):** live-view works for a browser on the server box only — `go2rtc_base` is `127.0.0.1`, so remote/LAN viewers need the planned WS-reverse-proxy through zoomy's authed origin (which would also let us drop `origin: "*"` and its residual localhost-origin CSRF); that proxy is the clear next step. Adversarial 2-lens review (security+web): the cache-poisoning bug was caught and fixed) |
| 40 | Config backup & restore (export/import) | Blue Iris export, Synology config backup, self-host migration | ✅ shipped (`GET /api/backup` downloads a JSON snapshot of the *configuration* — cameras, the settings blob, and alarm rules — with a `Content-Disposition` attachment header; `POST /api/restore` imports it. Restore is **additive/non-destructive**: settings are replaced wholesale, but a camera/alarm whose name already exists is left untouched, and per-camera alarm scopes are **re-pointed by camera name** so they target the right camera on the new box (camera ids differ between instances). Recordings/events/faces are intentionally excluded (data, not config). Settings page gains a Backup & restore card (download link + file-upload restore with a confirm + summary). **Security hardening from the review:** camera `source`/`detect_source` (which flow verbatim into go2rtc's generated YAML) are now rejected if they contain control characters at every entry point — `add_camera`, `patch_camera`, and `restore` — closing a newline→`exec:` YAML-injection→RCE vector (a malicious imported backup could otherwise have run commands on go2rtc restart); `exec:`/`ffmpeg:` schemes stay allowed since they're legitimate. go2rtc's config writer also strips control chars defensively. Unit-tested (source validation, group cap); live-validated end-to-end across two instances (round-trip of cameras+groups+alarms+a sentinel setting into a fresh box; idempotent re-restore; alarm remap-by-name with shifted ids; injection rejected on POST+PATCH). Adversarial review: 1 high (the injection, fixed) + 1 medium (alarm remap, fixed) + lows) |
| 39 | Camera groups + multi-cam Wall view | Blue Iris camera groups, UniFi/Synology layouts | ✅ shipped (optional per-camera `group` tag (nullable `group_name` column, additive migration); the Live grid gains group filter tabs — All / each group / Ungrouped, persisted in localStorage with a stale-selection reset; the Cameras page gets an inline group editor (commit on blur/Enter, empty clears) + `<datalist>` autocomplete + an add-form group field. Bonus: `patch_camera` now restarts go2rtc **only** when a stream-relevant field changed (name/source/detect_source/enabled), so metadata-only edits — group, detect, record, zones — no longer blip every live stream (a step toward the long-standing "don't restart go2rtc on CRUD" goal). Server CRUD live-validated (group set/clear/list; restart-gating verified: a group-only PATCH left go2rtc untouched at restart-count 4 while an `enabled` toggle bumped it to 5). build/clippy/test/web-build green; adversarial 2-lens review clean (4 nits, 0 real defects); group label capped at 64 chars) |
| 38 | Opt-in GenAI event captions (local Ollama or cloud) for review + search | Frigate+LLM community add-ons, UniFi AI descriptions | ✅ shipped (a captioner worker thread (off the detection path) sends an event snapshot to an Ollama-compatible vision model and writes a one-line description onto the event; displayed on cards and stored for search. **Explicit opt-in, OFF by default** — default endpoint is localhost Ollama so nothing leaves the machine unless the user points it at a cloud URL (bearer-key supported). Unit-tested request builder + Ollama/OpenAI response parsing; live captioning needs a running Ollama to validate) |
| 37 | Face recognition as per-camera opt-in + labeled-faces management | Frigate/UniFi face rec (opt-in, gated behind person detection) | ✅ shipped (per-camera `face_recognize` override — enable matching only where wanted (e.g. front door), inheriting the global switch otherwise; still gated behind person detection. Enrolled-identity rename (PATCH /api/faces/:id → relabels all that person's embeddings) added to the existing Faces management UI alongside enroll/forget. Builds on shipped face rec #14) |
| 36 | LPR vehicles-of-interest: plate allow/deny lists | Frigate LPR + UniFi/Reolink plate alerts | ✅ shipped (case-insensitive substring allow/deny lists tolerant of partial OCR; a deny-list "vehicle of interest" read fires a guaranteed high-priority push independent of alarm rules; Events badges plates ⚠ of interest / ✓ known. Unit-tested `plate_status` (deny > allow). Builds on the shipped LPR #14) |
| 35 | Duress/help hand signal (silent panic) + touchless PTZ control | Differentiator — personal-safety angle no NVR ships | ✅ shipped (a configurable `gesture_duress` signal always fires even when not armed; the gesture event is flagged duress → ntfy pushes go out at max urgency with a siren tag + "🚨 DURESS" title, plus a guaranteed direct push to the health ntfy topic so it alerts with zero alarm-rule setup. Touchless PTZ: on a PTZ camera, an open palm steers toward the hand's position and a fist stops — hands-free control from the Signals page. Backend build/clippy/test green; live overlay/PTZ need a webcam+camera to validate) |
| 34 | Per-camera detector/accelerator assignment + FPS governance + HW-accel encode + live metrics | Frigate detector groups, BI per-camera limits, UniFi perf surface | ✅ shipped (pipeline keeps one ONNX session per (model, accelerator, conf, iou); per-camera `model` override, `force_cpu` GPU/CPU assignment, and `poll_ms` FPS cap (resource governance); inference latency + accelerator + model recorded per camera and surfaced in a Perf column. Enhanced-retention re-encode can use NVENC/QuickSync/VideoToolbox with automatic CPU fallback (`hwaccel` setting). Build/clippy/test green; HW paths need GPU hardware to validate) |
| 33 | Restreaming fan-out: per-viewer WebRTC / MSE / MJPEG transport selection | go2rtc restreaming (the engine we already supervise) | ✅ shipped (the single-upstream→many-clients fan-out is inherent to the supervised go2rtc — this exposes a per-viewer transport picker: WebRTC for lowest latency with automatic MSE fallback, MSE for WebRTC-blocked/TCP-only networks, MJPEG for maximum compatibility. Preference persists in localStorage and applies to the Live grid + camera detail. No extra camera load) |
| 32 | Event review: zone tag on events + zone/time filters + Explore counts + thumbnails | Frigate Explore/review, UniFi timeline filters | ✅ shipped (events are tagged with the required-zone they occurred in; Events page filters by object/zone/camera/face/gesture/plate and a from–to time window (server-side `after`/`before`); a Frigate-style "Explore" header shows per-object counts as quick-filter chips; grid images load via on-the-fly cached `?w=` thumbnails. Existing Timeline scrubber retained. Unit-tested zone+time queries) |
| 31 | Home Assistant MQTT auto-discovery + detection-state entities + webhook templating | Frigate HA integration, BI alarm-server templating | ✅ shipped (publishes retained HA discovery configs → a `binary_sensor` per (camera, object) with auto-ON/OFF + a per-camera last-detection `sensor`, grouped under one HA device; re-published when the camera/label set changes. `{prefix}/{camera}/{slug}/state` ON/OFF topics + per-camera event JSON. Webhook body templating with `{{placeholder}}` substitution (JSON-escaped) across per-event + alarm + gesture webhooks; documented topic/payload/template schema in docs/03. Unit-tested template render; needs a live broker re-validation) |
| 30 | Anti-fatigue notifications: per-rule cooldown + snooze, clip-link & priority push | Frigate notify cooldowns, UniFi snooze, Reolink priority | ✅ shipped (per-rule `cooldown_secs` enforced via a shared in-memory throttle across pipeline/audio/gesture dispatch; manual `snooze`/`wake`; ntfy pushes carry `X-Priority` and, when a public base URL is set, tap-through "View clip"/"Snapshot" action links. Unit-tested. **Native WebPush deferred** — needs a VAPID/web-push crypto+HTTP dep stack (isahc/openssl) and browser+push-service validation that can't be exercised in this environment; ntfy already delivers thumbnail+clip push) |
| 29 | Polygon detection zones (required/ignore) + privacy masks + object-size filters | Frigate zones/masks, BI object-size, UniFi privacy zones | ✅ shipped (per-camera polygon zones with even-odd point-in-polygon test, required vs ignore + per-zone label scoping; privacy-mask polygons blacked out of the frame before motion/detect/snapshot; min/max object-area gate; visual editor draws on a same-origin `/api/cameras/:id/frame.jpg` still; legacy rectangle ignore_zones still honored. Unit-tested; needs live in-browser editor validation) |
| 28 | Hand-signal recognition (live 21-landmark mesh + held-gesture triggers) | Differentiator — no incumbent ships this; MediaPipe Hands | ✅ shipped (Signals page: GPU hand-landmark tracking in-browser via MediaPipe Tasks Vision, draws the skeletal mesh, classifies open-palm/fist/victory/point/thumb-up/down/I-love-you; a held armed signal POSTs `/api/gesture` → a `gesture` event with a context snapshot that fires matching alarm rules — a silent hand-signal "panic button". `gesture` crate has a pure, unit-tested geometric classifier + name taxonomy; per-camera toggle, Settings knobs, Alarm `gesture` condition, Events chip/filter. Needs in-browser live validation against a webcam) |

Sources: [Frigate docs](https://docs.frigate.video/), [Frigate releases](https://github.com/blakeblackshear/frigate/releases),
[Frigate review system](https://docs.frigate.video/configuration/review/),
[Blue Iris](https://blueirissoftware.com/), [BI alarm-server webhooks](https://wiki.instar.com/en/Frequently_Asked_Question/BlueIris_v5_http_alarmserver/),
[UniFi Protect G6 coverage](https://www.thesmarthomehookup.com/unifi-protect-got-amazing/),
[BI vs UniFi comparisons](https://ipcamtalk.com/threads/blue-iris-vs-unifi-protect.67055/).
