# Rust
/target
**/target
# keep unlocked for a multi-binary workspace during early dev
Cargo.lock

# Bundled third-party binaries (downloaded by scripts, not committed)
/bin/go2rtc
/bin/go2rtc.exe
/bin/ffmpeg
/bin/ffmpeg.exe
/bin/cloudflared
/bin/cloudflared.exe

# Model weights (downloaded, not committed)
*.onnx
*.pt

# Generated runtime config / recordings / scratch
/config/go2rtc.generated.yaml
/recordings/
/data/
*.log

# Downloaded / generated test media (see spike READMEs)
/bus.jpg
/sample.mp4
/driveway.mp4
/snapshot.jpg

# Web UI build artifacts
/web/node_modules/
/web/dist/

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/

# webhook test listener output
/webhook_log.jsonl
/mqtt_sub.log

# CLIP tokenizer (downloaded with the smart-search models)
/clip_tokenizer.json

# LPR aux files (downloaded with the plate models)
/plate_dict.txt
/plate_config.json
/plate-sample-*.jpg
/lpr-test.jpg
/plate.mp4
/plate2.mp4
/dist/
/yamnet_class_map.csv

web/tsconfig.tsbuildinfo

# Whisper STT model (downloaded, not committed) + test sample
/ggml-*.bin
/jfk.wav

# License fulfilment runtime artifacts (contain keys + buyer emails — never commit)
/fulfilment-spool/
/fulfilment-ledger.json
/fulfilment-ledger.json.tmp
__pycache__/
*.pyc

# License signing seed — THE root of trust. Never commit. Move offline.
/SECRET-license-seed.txt
